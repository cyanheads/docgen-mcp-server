{
  "name": "docgen-mcp-server",
  "version": "0.1.0",
  "description": "MCP server that renders agent content into downloadable documents: HTML/markdown/template to PDF, tabular rows to xlsx, and AcroForm PDF fill — delivered as a download URL or inline base64.",
  "author": {
    "name": "cyanheads"
  },
  "homepage": "https://github.com/cyanheads/docgen-mcp-server",
  "repository": "https://github.com/cyanheads/docgen-mcp-server",
  "license": "Apache-2.0",
  "mcpServers": {
    "docgen-mcp-server": {
      "command": "npx",
      "args": ["-y", "docgen-mcp-server"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio"
      }
    }
  }
}
